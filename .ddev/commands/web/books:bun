#!/bin/bash
## Description: Run bun commands in the Books theme directory
## Usage: books:bun <bun-command> [args]
## Example: "ddev books:bun install" or "ddev books:bun run build"

if [ ! -d "/var/www/html/web/themes/custom/books" ]; then
    echo "Error: Theme 'books' not found in web/themes/custom/"
    exit 1
fi
cd /var/www/html/web/themes/custom/books || exit
exec bun "$@"
