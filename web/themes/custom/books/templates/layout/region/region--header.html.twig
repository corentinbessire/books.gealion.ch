<nav class="nav-antiquarian bg-cream border-b-2 border-stone-300 relative py-4 md:py-5" x-data="{ mobileMenuOpen: false }">
  <div class="container px-6 mx-auto flex items-center justify-between">
    <!-- Logo -->
    <a href="{{ path('<front>') }}" class="flex items-center gap-3 group">
      {{ include('books:icon', {
          name: 'book-open', size: 'w-10 h-10',
          extra_classes: 'text-burgundy transition-transform duration-300 group-hover:-rotate-[5deg]',
        }) }}
    </a>

    <!-- Desktop Navigation -->
    <div class="hidden md:flex items-center gap-2">
      {{ elements.books_main_menu }}
    </div>

    <!-- Mobile Menu Button -->
    <button
      class="md:hidden p-2 text-ink-muted hover:text-burgundy transition-colors"
      @click="mobileMenuOpen = !mobileMenuOpen"
      :aria-expanded="mobileMenuOpen">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path x-show="!mobileMenuOpen" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
        <path x-show="mobileMenuOpen" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
      </svg>
    </button>
  </div>

  <!-- Mobile Menu -->
  <div
    class="md:hidden overflow-hidden transition-all duration-300 ease-out"
    x-show="mobileMenuOpen"
    x-transition:enter="transition ease-out duration-200"
    x-transition:enter-start="opacity-0 -translate-y-2"
    x-transition:enter-end="opacity-100 translate-y-0"
    x-transition:leave="transition ease-in duration-150"
    x-transition:leave-start="opacity-100 translate-y-0"
    x-transition:leave-end="opacity-0 -translate-y-2">
    <div class="container px-6 mx-auto py-4 border-t border-stone-300">
      {{ elements.books_main_menu }}
    </div>
  </div>
</nav>
