name: 'Books - Book Management (retiring)'
type: module
description: 'Empty shell kept only so the update-hook history survives and old service IDs keep resolving. Its code now lives in books_catalog, books_cover and books_hardcover. Remove once every consumer uses the new IDs.'
package: Books
core_version_requirement: ^10 || ^11
dependencies:
  - books_catalog:books_catalog
  - books_cover:books_cover
  - books_hardcover:books_hardcover
