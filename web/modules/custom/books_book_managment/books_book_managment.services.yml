services:
  # Aliases so consumers not yet migrated keep resolving. Removed in the
  # follow-up that retires this module.
  books.books_utils:
    alias: books_catalog.books
  books.cover_download:
    alias: books_cover.downloader
  books.hardcover:
    alias: books_hardcover.client
